package net.thejadeproject.ascension.constants;

public enum SkillRemoveSource {
    FORCE_REMOVE, //removes everything
    REBIRTH, // bypasses fixed but not permanent
    REALM_CHANGE,// anything to do with techniques, mainly canceled by fixed
    PHYSIQUE_CHANGE, //anything to do with techniques, mainly canceled by fixed
}
